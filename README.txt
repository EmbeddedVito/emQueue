@author Mannone Vito

@brief implementation of a FIFO queue

This implementation does not rely on a specific implementation either of a data structure, operating system (locking mechanism) or dynamic memory allocation algorithm. All these function can and must be defined in the mQueuePort.h file.
